{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "inova-gastro-360-web",
  "compatibility_date": "2025-06-14",
  "assets": {
    "directory": "./out",
    "html_handling": "drop-trailing-slash",
    "not_found_handling": "404-page"
  }
  // Produção VPS: Tunnel → nginx :9088. NÃO registrar custom_domain aqui —
  // conflita com cloudflared e mantém deploy antigo no edge (POST /api → 405).
  // Ver infra/hetzner/CLOUDFLARE-CUTOVER.md
}
